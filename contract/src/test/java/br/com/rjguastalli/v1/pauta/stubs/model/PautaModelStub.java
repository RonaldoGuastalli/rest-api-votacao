package br.com.rjguastalli.v1.pauta.stubs.model;import br.com.rjguastalli.pauta.model.PautaModel;import java.time.Instant;import java.time.LocalDateTime;import java.time.ZoneId;import java.util.Arrays;public class PautaModelStub {    private static final String ZONE_ID = "America/Sao_Paulo";    public static PautaModel pautaModelMock() {        return PautaModel.builder()                .id(1L)                .descricao("Indice de satisfação dos clientes no trimestre.")                .dataCadastroPauta(LocalDateTime.ofInstant(Instant.now(), ZoneId.of(ZONE_ID)))                .dataDesativacao(null)                .sessoes(Arrays.asList())                .build();    }}